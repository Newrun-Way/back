services:
  backend:
    build: .
    container_name: owpml_backend
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./requirements.txt:/app/requirements.txt
    restart: unless-stopped
